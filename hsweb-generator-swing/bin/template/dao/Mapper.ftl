package ${packageName}.dao.${module};

import org.hsweb.web.dao.GenericMappe;
import ${packageName}.po.${module}.${beanName};

/**
* ${beanRemark!''}数据映射接口
* Created by hsweb-generator ${.now!''}
*/
public interface ${beanName}Mapper extends GenericMapper<${beanName},String> {

}
